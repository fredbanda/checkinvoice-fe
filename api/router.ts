import companyEndpoints from "./company";
import checkEndpoints from "./check";
import invoiceEndpoints from "./invoice";

const endpoints = {
    company: companyEndpoints,
    check: checkEndpoints,
    invoice: invoiceEndpoints,
};

export default endpoints;